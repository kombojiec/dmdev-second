spring:
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5433/library
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    format_sql: true
    hibernate:
      ddl-auto: validate